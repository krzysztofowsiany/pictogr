<Window x:Class="PictOgr.SplashScreen.Views.SplashScreenView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PictOgr" Height="200" Width="400"
		Background="PaleGreen"
		>
	<Window.Resources>
		<ResourceDictionary>
			<ResourceDictionary.MergedDictionaries>
				<ResourceDictionary Source="../../Style/Style.xaml"/>
			</ResourceDictionary.MergedDictionaries>
		</ResourceDictionary>
	</Window.Resources>
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="*"/>
			<RowDefinition Height="50"/>
			<RowDefinition Height="25"/>
		</Grid.RowDefinitions>
		<StackPanel Grid.Row="0">
		</StackPanel>
		<StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
			<Button Command="{Binding ExitBoxCommand}" Margin="10" Width="60" Style="{StaticResource myStyle}">Exit</Button>
		</StackPanel>
		<StackPanel Grid.Row="2" HorizontalAlignment="Right">
			<Label Content="{Binding ApplicationInformation.Version}" />
		</StackPanel>


	</Grid>

</Window>
